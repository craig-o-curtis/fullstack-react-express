import { createBrowserHistory } from 'history';

// creates object that React-Router can use to look at past and current history
// this file exports history instance all other files can use
export const history = createBrowserHistory();